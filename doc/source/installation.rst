============
Installation
============

At the command line::

    $ pip install networking-baremetal

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv networking-baremetal
    $ pip install networking-baremetal
